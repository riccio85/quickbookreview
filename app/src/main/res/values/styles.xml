<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/book_pending</item>
        <item name="colorPrimaryDark">@color/list_section_background</item>
    </style>
    <!-- Main activity theme. -->
    <style name="MainTheme" parent="AppTheme">
        <item name="actionBarStyle">@style/ActionBar.Solid.NoTitle</item>
    </style>

    <!-- main activity action bar styles -->
    <style name="ActionBar.Solid.NoTitle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="displayOptions">useLogo|showHome</item>
        <item name="logo">@drawable/logo</item>
    </style>

    <style name="BookDetailSubHeading">
        <item
            name="android:layout_gravity">right</item>
        <item
            name="android:layout_height">wrap_content</item>
        <item
            name="android:layout_width">wrap_content</item>
        <item
            name="android:layout_marginRight">7dp</item>
        <item
            name="android:textStyle">bold</item>

    </style>

    <style name="BookDetailContent">
        <item
            name="android:layout_height">wrap_content</item>
        <item
            name="android:layout_width">wrap_content</item>
        <item
            name="android:paddingBottom">7dp</item>
        <item
            name="android:paddingRight">7dp</item>
        <item
            name="android:textColorLink">@color/link</item>
    </style>

<!-- to use for personalized animated progress Dialog bar  NOT YET USED -->
    <style name="TransparentProgressDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
